function adjustTopicHeight(){var i=$("#topics-container").height();$("#topics").css({height:i})}var windowTimer=null;$(window).width()>=mediumBreakPoint&&adjustTopicHeight(),$(window).bind("resize",function(){$(window).width()>=mediumBreakPoint?(windowTimer&&clearTimeout(windowTimer),windowTimer=setTimeout(adjustTopicHeight,150)):$("#topics").css({height:"auto"})});